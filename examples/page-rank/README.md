# Page-Rank in PiCo

Valid input files are list of `\n`-separeted edges, where each edge is a pair of space-separated numbers.

Files from the SNAP repository can be easily adapted by removing heading comments:
https://snap.stanford.edu/data/index.html